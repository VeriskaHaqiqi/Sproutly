[phases.setup]
nixPkgs = ["php82", "php82Extensions.intl", "php82Extensions.pdo_mysql", "composer"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.build]
cmds = ["php artisan optimize"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT}"